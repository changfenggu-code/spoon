{
  "permissions": {
    "allow": [
      "Bash(gh release view --repo apple/pkl-vscode --json tagName,assets --jq '{tag: .tagName, files: [.assets[].name]}')",
      "Bash(powershell -File d:/projects/spoon/skills/scripts/run-cmd.ps1 gh release view --repo apple/pkl-vscode --json tagName,assets --jq \"{tag: .tagName, files: [.assets[].name]}\")"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/sync-docs.sh"
          }
        ]
      }
    ]
  }
}
